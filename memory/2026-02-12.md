# 2026-02-12

## Proxmox Monitoring Setup Complete

Set up daily Proxmox health checks with Bitwarden integration and Telegram alerts.

### Expected Stopped VMs/Containers (do not alert)
These are intentionally stopped:
- **VMs:** UbuntuDesktop, Debian-AMP, haosova-7.2
- **Containers:** teamspeak (one instance), antigravity-proxy, pipeshub, chainlit, pterodactyl

### Nodes
- pve-local: main node (56 cores, 126GB RAM)
- pve-remote: secondary node (12 cores, 126GB RAM)

### API Token Management
Migrated all API keys to Bitwarden (8 items total). Bitwarden automation credentials stored at `~/.openclaw/.bw-credentials`.

Helper script: `bw-get "Item Name"` retrieves secrets from Bitwarden.

### OpenClaw Bitwarden Setup
- Credentials file: `/home/linus/.openclaw/.bw-credentials`
- rbw binary: `~/.local/bin/rbw`
- **Use `bw` helper script, not `rbw` directly** - Run: `bw-get "Item Name"`
